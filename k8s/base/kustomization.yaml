apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base resources shared across all environments.
# Overlays reference this base and apply environment-specific patches.
resources:
  - deployment.yaml
  - service.yaml
  - hpa.yaml

