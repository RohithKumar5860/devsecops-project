apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  labels:
    app: devsecops-app
type: Opaque
# Base secret manifest – no values committed here.
# Each overlay provides its own APP_SECRET_KEY via its own secret.yaml.
# To create a secret manually:
#   kubectl create secret generic app-secrets \
#     --from-literal=APP_SECRET_KEY="<value>" \
#     -n <namespace> --dry-run=client -o yaml | kubectl apply -f -
data: {}
