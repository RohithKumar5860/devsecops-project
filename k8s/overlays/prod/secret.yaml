apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  labels:
    app: devsecops-app
    environment: prod
type: Opaque
data:
  # Production placeholder secret – base64("prod-placeholder")
  # CRITICAL: Replace with a real secret via external secrets manager
  # (e.g. HashiCorp Vault, Sealed Secrets, AWS Secrets Manager) before production use.
  APP_SECRET_KEY: "REPLACE_ME"
